; PHP-FPM per-directory overrides. Required so screenshots / file uploads up
; to UPLOAD_MAX_FILE in .env (50 MB) actually reach $_FILES.
upload_max_filesize = 60M
post_max_size       = 60M
max_file_uploads    = 20
memory_limit        = 128M
